export async function deployLiquidityMining(vault: string) {
  try {
    /**
     * Going through ordered steps neeed to deploy and setup/configure the Liquidity Mining system
     */
    // Auth Adapter. Vault should already have this
  } catch (error) {
    console.error(error);
    process.exitCode = 1;
  }
}
